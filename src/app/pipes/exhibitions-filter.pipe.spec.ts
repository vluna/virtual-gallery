import { ExhibitionsFilterPipe } from './exhibitions-filter.pipe';

describe('ExhibitionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExhibitionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
